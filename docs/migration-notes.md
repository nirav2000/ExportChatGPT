# Migration Notes

Initial schema version: `1`.

Planned migrations:
- v2: separate `SourceRecord` table for provenance and retry state.
- v3: optional project tags and manual chat reclassification audit log.
