{
  "name": "@project-archivist/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "vite",
    "frontend:build": "vite build",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@project-archivist/exporter": "workspace:*",
    "@project-archivist/schema": "workspace:*",
    "@tauri-apps/api": "2",
    "@tauri-apps/plugin-dialog": "^2.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "vite": "^6.1.0"
  }
}
